class Eel {
  /* 
  This  is for testing online.
  Here are placeholders of the eel functions
  so that the front-end can be tested with 'npm run dev'
  */

  on_loaded() {
    console.log("on_loaded placeholder");
  }
  expose(func, name) {
    console.log("expose placeholder", func, name);
  }
}

const eel = new Eel();
